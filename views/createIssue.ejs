<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create New Issue for <%= project.name %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #ff0000, #ff7f00, #ffff00, #7fff00, #00ff00, #00ff7f, #00ffff, #007fff, #0000ff, #7f00ff, #ff00ff, #ff007f);
      background-size: 300% 300%;
      animation: gradient 15s ease infinite;
      color: #333; /* Adjust text color for better readability */
    }
    .form-container {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .btn-custom {
      background-color: #28a745;
      color: white;
    }
    .btn-custom:hover {
      background-color: #218838;
      color: white;
    }
    @keyframes gradient {
      0% { background-position: 0% 0%; }
      50% { background-position: 100% 100%; }
      100% { background-position: 0% 0%; }
    }
  </style>
</head>
<body>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <!-- Form and Back Button in the Same Box -->
        <div class="form-container p-4 border rounded shadow-sm mb-5">
          <!-- Form to Create New Issue -->
          <h2 class="text-center mb-3 text-primary">Create New Issue</h2>
          <h3 class="text-center mb-4 text-secondary"><%= project.name %></h3>
          <form action="/issues/create" method="post">
            <div class="mb-3">
              <label for="title" class="form-label">Issue Title</label>
              <input type="text" name="title" id="title" class="form-control" placeholder="Issue Title" required>
            </div>
            <div class="mb-3">
              <label for="description" class="form-label">Description</label>
              <textarea name="description" id="description" class="form-control" placeholder="Description" rows="4" required></textarea>
            </div>
            <div class="mb-3">
              <label for="labels" class="form-label">Labels (comma separated)</label>
              <input type="text" name="labels" id="labels" class="form-control" placeholder="Labels (comma separated)">
            </div>
            <div class="mb-3">
              <label for="author" class="form-label">Author</label>
              <input type="text" name="author" id="author" class="form-control" placeholder="Author" required>
            </div>
            <input type="hidden" name="project" value="<%= project._id %>">
            <button type="submit" class="btn btn-custom w-100">Create Issue</button>
          </form>

          <!-- Link to go back -->
          <div class="text-center mt-4">
            <a href="/issues/<%= project._id %>" class="btn btn-secondary">Back to Issues</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS (Optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
